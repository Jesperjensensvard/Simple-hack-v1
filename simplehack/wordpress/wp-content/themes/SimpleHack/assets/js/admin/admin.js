jQuery(document).ready(function($){

	/* var field = document.querySelectorAll('.acf-field'),
    	i;
	if(field.length) {
		for(i = 0; i < field.length; i++) {
			var thisField = field[i],
				fieldName = thisField.dataset.name,
				fieldLabel = document.createElement('span');
			fieldLabel.style.cssText = 'color:#999;font-weight:normal;';
			fieldLabel.innerHTML = ' [' + fieldName + ']';
			thisField.querySelector('.acf-label label').appendChild(fieldLabel);
		}
	}

	var layout = document.querySelectorAll('.acf-field .layout'),
    	i;
	if(layout.length) {
		for(i = 0; i < layout.length; i++) {
			var thislayout = layout[i],
				layoutName = thislayout.dataset.layout,
				layoutLabel = document.createElement('span');
			layoutLabel.style.cssText = 'color:#999;font-weight:normal;';
			layoutLabel.innerHTML = ' [' + layoutName + ']';
			thislayout.querySelector('.acf-fc-layout-handle').appendChild(layoutLabel);
		}
	} */

	/* acf.add_filter('color_picker_args', function( args, $field){
		args.palettes = ['#fff','#e8f0d2', '#98c21d', '#f8e6d8', '#ebe8f5', '#f8f7fb' , '#fef8ed' ,'#ffeaca']

		// return
		return args;
	}); */

});
